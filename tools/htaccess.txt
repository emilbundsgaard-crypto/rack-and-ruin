# --- ClouterX -----------------------------------------------------------
#
# 1. Some servers hand .js to the browser as plain text. Browsers refuse to
#    run modules that are not text/javascript, and the page comes up blank.
AddType text/javascript .js

# 2. Do not cache the game files.
#
#    An earlier version of this file cached them for a day, which meant that
#    uploading a fix changed nothing for anybody who had already opened the
#    site: their browser kept serving the old code for the next 24 hours.
#
#    The whole game is under 400 KB and the browser revalidates with a
#    conditional request, so it still only re-downloads what actually
#    changed. Correctness beats a saved kilobyte here.
<IfModule mod_headers.c>
  <FilesMatch "\.(js|css|html)$">
    Header set Cache-Control "no-cache, must-revalidate"
  </FilesMatch>
</IfModule>

# 3. Never hand out the files behind the visitor counter.
#
#    track.php and stats.php are meant to be requested. config.php and lib.php
#    are not: PHP pulls them in from disk, which never goes through the web
#    server at all. Denying them costs nothing and means that if PHP is ever
#    off — a moved host, a broken upgrade — the server cannot serve the stats
#    password to whoever asks for it as plain text.
<FilesMatch "^(config|config\.example|lib)\.php$">
  Require all denied
  <IfModule !mod_authz_core.c>
    Deny from all
  </IfModule>
</FilesMatch>

# 4. And never the visitor data itself.
RedirectMatch 404 ^/api/data/
